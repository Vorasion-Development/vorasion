{
  "category": "commands"
}
---
import { Command } from '@sapphire/framework'
import { ApplyOptions, ChatInputCommand, createCommandMixin } from '@vorasion/decorators'

@ApplyOptions<Command.Options>({
  name: '{{name}}',
})
@ChatInputCommand({
  name: '{{name}}',
  description: 'A simple slash command that replies with a message.',
})
export class {{name}}Command extends createCommandMixin(Command) {
  public override async chatInputRun(interaction: Command.ChatInputCommandInteraction) {
    await interaction.reply(`Hello from ${this.name}!`)
  }
}
